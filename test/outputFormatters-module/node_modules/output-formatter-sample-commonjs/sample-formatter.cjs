// @ts-check

"use strict";

module.exports = (options, params) => {
  const { logError, results } = options;
  for (const result of results) {
    const { fileName, lineNumber, ruleNames } = result;
    logError(`cjs: ${fileName} ${lineNumber} ${ruleNames.join("/")}`);
  }
};
